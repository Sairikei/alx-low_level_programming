#include "holberton.h"

/**
 *_islower - return 1 if upper case else 0
 *@c: integer to be tested
 *Return: Always 0
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0)
	}
}
